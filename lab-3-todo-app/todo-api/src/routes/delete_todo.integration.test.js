const request = require("supertest");
const server = require("../server");
const Todo = require("../models/todo");

describe("Delete todos from the Database", () => {
  it("Deletes something sucesfully", () => {});
  it("Gives an error if we can't delete", () => {});
});
